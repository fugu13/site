# Heading Navigation

## Jumping between sections on the home page

A reader using a screen reader's heading-navigation shortcut steps from the page's main heading, "Russell Duhon", directly to the "Blog" section heading, in strict, unbroken order — no heading level is skipped in between. The page's visible layout doesn't change: the "Blog" heading looks the same size it always has.
